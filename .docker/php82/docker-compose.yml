version: "3.7"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: php82-dto
    container_name: php-dto
    restart: unless-stopped
    working_dir: /var/www/
    volumes:
      - ../../:/var/www
    networks:
      - default

networks:
  default:
    name: php-dto
